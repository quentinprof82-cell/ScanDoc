let stream=null;export async function open(){if(!navigator.mediaDevices?.getUserMedia)throw Error('unsupported');stream=await navigator.mediaDevices.getUserMedia({video:{facingMode:{ideal:'environment'},width:{ideal:1920},height:{ideal:1080}},audio:false});return stream}export function stop(){stream?.getTracks().forEach(t=>t.stop());stream=null}export async function photo(video){if(!video.videoWidth)throw Error('notready');const c=document.createElement('canvas');c.width=video.videoWidth;c.height=video.videoHeight;c.getContext('2d').drawImage(video,0,0);return new Promise(r=>c.toBlob(r,'image/jpeg',.96))}
