import{open as openCam,stop,photo}from'./camera.js';import{prepare,render,hit,move,autodetect,full,exportPage}from'./editor.js';
let modal,video,editor,drag=-1;const $=s=>document.querySelector(s);export function close(){stop();modal?.remove();modal=null}
export async function takePhoto(onPage){try{const stream=await openCam();modal=document.createElement('div');modal.className='modal';modal.innerHTML='<section class="capture-screen"><div class="capture-top"><b>Prendre une photo</b><button class="capture-close">✕</button></div><div class="capture-stage"><video autoplay playsinline muted></video></div><div class="capture-help">Cadrez le document puis appuyez sur PHOTO.</div><div class="capture-bottom"><button class="capture-side" id="gal">🖼 Galerie</button><button class="photo-button" id="shot"><i></i></button><button class="capture-side" id="cancel">Annuler</button></div></section>';document.body.appendChild(modal);video=modal.querySelector('video');video.srcObject=stream;await video.play();modal.querySelector('#shot').onclick=async()=>{const b=await photo(video);stop();modal.remove();modal=null;showCrop(b,onPage)};modal.querySelectorAll('#cancel,.capture-close').forEach(b=>b.onclick=close);modal.querySelector('#gal').onclick=()=>{stop();close();$('#fileInput').click()}}catch(e){$('#fileInput').click()}}
async function showCrop(blob,onPage){editor=await prepare(blob);modal=document.createElement('div');modal.className='modal';modal.innerHTML='<section class="crop-screen"><div class="capture-top"><b>Recadrer le document</b><button class="capture-close">✕</button></div><div class="crop-stage"><canvas class="crop-canvas"></canvas></div><div class="crop-toolbar"><button id="auto">🔎 Détecter les coins</button><button id="full">↔ Toute l\'image</button></div><div class="crop-actions"><button class="crop-cancel" id="cc">Annuler</button><button class="crop-confirm" id="use">✓ Utiliser cette page</button></div></section>';document.body.appendChild(modal);const cv=modal.querySelector('canvas'),draw=()=>render(cv,editor);requestAnimationFrame(draw);modal.querySelector('#auto').onclick=()=>{if(!autodetect(editor))alert('Coins non détectés. Vous pouvez les placer manuellement.');draw()};modal.querySelector('#full').onclick=()=>{full(editor);draw()};modal.querySelector('#cc').onclick=close;modal.querySelector('.capture-close').onclick=close;modal.querySelector('#use').onclick=()=>{const q=localStorage.getItem('quality')||'high';onPage({data:exportPage(editor,q==='small'?.72:q==='medium'?.84:.93)});close()};const p=e=>{if(e.type==='pointerdown'){drag=hit(e,cv,editor)}else if(e.type==='pointermove'&&drag>=0){e.preventDefault();move(e,cv,editor,drag);draw()}else drag=-1};['pointerdown','pointermove','pointerup','pointercancel'].forEach(t=>cv.addEventListener(t,p,{passive:false}))}
export async function openCrop(blob,onPage){await showCrop(blob,onPage)}
